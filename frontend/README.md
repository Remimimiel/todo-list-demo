1. Download denpendency

    ```sh {.line-numbers}
    # cnpm install
    npm install
    ```

2. Get start

    ```sh {.line-numbers}
    yarn start
    ```

3. Open browser and visit `http://localhost:3000`
